# Deployment manifests

No Project GAS deployment is approved or recorded yet.

Future manifests must include the chain ID, deployment transaction, bytecode
hash, constructor/config hash, compiler/toolchain versions, role manifest and
post-deployment readback. Base mainnet manifests require explicit release
approval after testnet, adversarial testing, security review and Phase 9 closure.

